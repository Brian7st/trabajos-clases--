let ancho = parseInt(prompt("ingrese el ancho del lote"));
let largo = parseInt(prompt("ingrese el largo del lote"));

let perimetro = (ancho + largo) * 2;
alert(`lacantidad de cerca que se le va es de ${perimetro}`);