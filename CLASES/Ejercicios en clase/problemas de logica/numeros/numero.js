let numero = parseInt(prompt("ingrese el numero para hacer la lista"))

function listaDeNumeros(n) {
        for (let i = 0; i < n; i--) {
            let arreglo = array[i];
          return arreglo  
        }
    };

let lista = listaDeNumeros(n);

alert(`la lista de numero es ${lista}`)