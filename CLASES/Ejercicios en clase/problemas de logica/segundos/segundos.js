let segundos = parseFloat(prompt("ingrese la cantidad de segundos"));
let minutos = segundos / 60;
let horas = minutos / 60;
let dias = horas / 24;
alert(`segundos ${segundos} conversion a minutos ${minutos} a horas ${horas} y a dias ${dias}`);